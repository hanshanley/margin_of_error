---
import MarginOfErrorCalculator from './MarginOfErrorCalculator';

interface Props {
	module: 'margin-of-error-calculator';
}

const { module } = Astro.props;
---

{module === 'margin-of-error-calculator' && <MarginOfErrorCalculator client:visible />}
